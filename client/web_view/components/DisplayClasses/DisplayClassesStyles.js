import styles from '../../containers/App/styles';

const { colors } = styles;

export default {
  displayClasses: {
    position: 'relative',
    zIndex: 1000,
    background: colors.grannyApple,
  },
};
