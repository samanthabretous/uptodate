import styles from '../../containers/App/styles';

const { colors } = styles;


export default {
  box: {
    position: 'absolute',
    right: 50,
    top: 50,
    zIndex: 1000,
    background: colors.gray,
  },
};
